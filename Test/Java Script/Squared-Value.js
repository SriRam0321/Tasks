// 7. Write a Javascript program to find the square of a number using Function expression

                    var num = 30;

                 var double = function(num) {
                 
                   return num * num;
                  
                    };
                    
                  var c = double(num);

                   console.log(c);